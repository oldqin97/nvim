/**
 * test
 *
 * @param {string} q - string
 * @param {string} w - string
 * @param {string} e - string
 */
function test(q, w, e) {
  console.log(q, w, e);
}

[1, 2, 3].map(item => {
  console.log(item);
});

let q = false;
// v4.2.9

test(1, 2, 3);
