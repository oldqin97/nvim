/**
 * 将输入参数的值打印到控制台并返回它。
 *
 * @param {*} q - 要打印和返回的输入参数。
 * @returns {*} 传入的相同输入参数。
 */
function qwe(q) {
  console.log(q);
  return q;
}

const q = () => {
  return 0;
};

console.log(q);

export default qwe;
